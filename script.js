document.addEventListener('DOMContentLoaded', (event) => {
  // Your JS code here
  window.addEventListener('scroll', () => {
    // We will add a class to the character icon when a section is in view
  });
});
